pipeline {
    agent any
    
    stages {
        stage("build code") {
            steps {
                script {
                   sh "tar -cvf a.tar index.html "
                   sh "ls -l a.tar"
                }
               }
            }
        stage("publish to nexus") {
            steps {
                script {
                    sh "curl -v -u admin:admin --upload-file a.tar http://67.202.11.212:8081/repository/UI-builds/dev/a.tar"
                }
            }
        }
        stage('run deploy job') {
steps {
build "qa-UI-deploy"
}
}
        
       
      
        
    }
    
}
